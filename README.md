# SMT-based Multi-Robot Task Allocation

The **S**MT-based **M**ulti-**R**obot **T**ask **A**llocation ("S-MrTa") is an SMT-based approach used in allocating tasks for multi-agent systems/scenarios.

## Installation

To install this command-line interface tool, run the following command:

```bash
uv pip install .
```
